# Test fixtures

* `references/` — small synthetic images used by the seed script for the two
  default subjects (Acme Logo, Rocket Mascot).
* `queries/` — query images used by the benchmark harness.

These are deliberately tiny so the repo stays small. Replace them with real
brand imagery before running production benchmarks.
